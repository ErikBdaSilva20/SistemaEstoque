// PROTEGIDO — mapa de componentes usado pelo editor por IA do hub (preview Sandpack).
//
// RECONSTRUÍDO: este ambiente não tem acesso ao editor por IA/Sandpack real do hub,
// então este registry é um stub sem efeito prático aqui — existe só para satisfazer
// o contrato de arquivos protegidos (docs/reference/Importantdoc.md §B7) e para não
// quebrar caso um editor real seja conectado depois. Preencha as entradas reais
// quando o editor do hub estiver disponível para este projeto.

export const componentRegistry: Record<string, unknown> = {};
